package com.sun.ray.hrdomain.configuration.property

class PropertyHandler{
    companion object {
        const val PREFIX = "ENC("
        const val POSTFIX = ")"
        const val MONGODB_PREFIX = "ENC:"
    }
}